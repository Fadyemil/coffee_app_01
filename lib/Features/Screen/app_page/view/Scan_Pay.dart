import 'package:flutter/material.dart';

class Scan_Pay extends StatelessWidget {
  const Scan_Pay({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Scan_Pay')
    );
  }
}